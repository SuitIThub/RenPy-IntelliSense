{
  "name": "renpy-doc-hover",
  "displayName": "Ren'Py Documentation Hover",
  "description": "Shows Ren'Py official documentation in hovers for .rpy / .rpym code.",
  "version": "0.1.0",
  "publisher": "Suit-Ji",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SuitIThub/RenPy-IntelliSense.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:renpy",
    "onLanguage:python"
  ],
  "main": "./out/extension.js",
  "files": [
    "out/**/*.js",
    "data/doc-index.json"
  ],
  "contributes": {
    "commands": [
      {
        "command": "renpyDocHover.openDefinition",
        "title": "Ren'Py Doc Hover: Open cross-reference at line"
      }
    ],
    "configuration": {
      "title": "Ren'Py Documentation Hover",
      "properties": {
        "renpyDocHover.fetchOnline": {
          "type": "boolean",
          "default": true,
          "description": "Fetch documentation text from https://www.renpy.org/doc/html/ when hovering (requires network). If false, only shows title and doc link from the index."
        },
        "renpyDocHover.cacheSize": {
          "type": "number",
          "default": 400,
          "minimum": 0,
          "description": "Maximum number of fetched doc pages to keep in memory (0 disables caching)."
        },
        "renpyDocHover.preferLocalDocstring": {
          "type": "boolean",
          "default": true,
          "description": "Show docstrings from your own definitions (triple-quoted or # comment blocks) in hovers when present."
        },
        "renpyDocHover.showOnlineDocsWithLocal": {
          "type": "boolean",
          "default": true,
          "description": "When a local docstring exists and the symbol is also in the Ren'Py manual, append the online excerpt below a separator."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "generate-index": "node scripts/generate-index.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
